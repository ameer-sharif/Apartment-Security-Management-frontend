import { Time } from "@angular/common"
export class Admin{
  emailId: string;
  password(password: any) {
    throw new Error('Method not implemented.');
  }
   adminId: number;
    
  
     
   constructor(
     emailId?: string,
     password?: string,
     adminId?: number,
     firstName?: string,
     lastName?: string,
     
     phoneNo?: string,
     
     token?: string,
   ){

   }
   
}

